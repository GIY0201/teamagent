# teamagent

Claude Code plugin that lets Claude consult Codex CLI and Gemini CLI as specialist subagents — no API keys, uses the user's existing CLI subscriptions.

**Status:** under active development (v0.1.0). See `../plan/2026-04-24-teamagent-design.md` for the full spec.

## Install

(TBD after Phase K.)

## Quickstart

(TBD after Phase K.)

## Requirements

- Node.js 20+
- Claude Code CLI (`claude`)
- Codex CLI (`codex`)
- Gemini CLI (`gemini`)
- Each CLI must already be logged in.
