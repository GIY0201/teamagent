{
  "name": "teamagent",
  "version": "0.1.0",
  "type": "module",
  "description": "Claude Code plugin for Codex + Gemini CLI orchestration.",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test tests/",
    "test:watch": "node --test --watch tests/",
    "test:integration": "TEAMAGENT_INTEGRATION=1 node --test tests/integration/"
  },
  "dependencies": {
    "tree-kill": "^1.2.2",
    "yaml": "^2.5.0"
  }
}
